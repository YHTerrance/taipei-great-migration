# Taipei Great Migration

This is a website that visualizes the flow of people during different times in Taipei (currently only using data from the MRT)

## Project Structure

* `EDA.ipynb`: Run exploratory data analysis on the taipei metro OD data.
* `interface`: Frontend interface for visualizing the taipei metro OD data.
* `backend`: Backend for serving the taipei metro OD data to the frontend.
